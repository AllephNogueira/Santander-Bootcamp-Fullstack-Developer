// Vamos ver as formas de colocar as variaveis dentro das Strings


const nome = "Alleph"
const sobrenome = "Nogueira"

console.log(`Bem vindo ${nome} ${sobrenome}`)

console.log('Bem vindo ' + nome + ' ' + sobrenome)

// Essas sao as duas formas de fazer.